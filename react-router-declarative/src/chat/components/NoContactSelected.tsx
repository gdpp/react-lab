const NoContactSelected = () => {
  return (
    <div className="flex-1 flex items-center justify-center text-muted-foreground ">
      Please select a client to start a conversation
    </div>
  );
};

export default NoContactSelected;
